# Rename to .env or .env.production and set your values
SEND_FROM_EMAIL=sliceitmanagement@gmail.com
SEND_FROM_NAME=SliceIt
# SENDGRID_API_KEY is a secret; set it via Secret Manager (do NOT commit secrets):
# firebase functions:secrets:set SENDGRID_API_KEY
